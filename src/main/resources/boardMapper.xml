<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 게시판 정보 처리 SQL -->
<mapper namespace="com.noback.group.dao.BoardMapper">
	<select id="getTotal" parameterType="string" resultType="int">
		select count(*)
		from noback_board
		<if test="_parameter != null">
			where
			upper(board_title) like '%' || upper(#{search}) || '%'
		</if>
	</select>
	
	<select id="listBoard" parameterType="string" resultType="board">
		select
			board_num
			, board_title
			, board_content
			, to_char(board_date, 'YY/MM/DD') board_date
			, board_writer
		from 
			noback_board
		<if test="_parameter != null">
		where
			upper(board_title) like '%' || upper(#{search}) || '%'
		</if>
		order by 
			board_num desc
	</select>
	
	<select id="board" parameterType="string" resultType="board">
		select * from noback_board
		where board_num = #{num}
	</select>
	
	<insert id="boardWrite" parameterType="board">
		insert into noback_board
		values(board_seq.nextval, #{board_title}, #{board_content}, (select employee_name from noback_member where employee_num = #{board_writer}), sysdate)
	</insert>
</mapper>





