<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 회원정보 처리 SQL -->
<mapper namespace="com.noback.group.dao.SignMapper">

<insert id="insertSign" parameterType="sign">
	insert into 
	noback_sign 
	(sign_sdate, sign_title, sign_content, sign_sender,sign_receiver
	<if test="originalfile != null and savedfile != null">
	originalfile, savedfile
	</if>) 
	values 
	(to_char(sign_sdate, 'YYYY-MM-DD') sign_sdate, #{sign_title}, #{sign_content}, #{sign_sender}, #{sign_receiver}
	<if test="originalfile != null and savedfile != null">
	#{originalfile}, #{savedfile}
	</if>)
</insert>

</mapper>



